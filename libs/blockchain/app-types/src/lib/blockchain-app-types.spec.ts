import { blockchainAppTypes } from './blockchain-app-types';

describe('blockchainAppTypes', (): void => {
  it('should work', () => {
    expect(blockchainAppTypes()).toEqual('blockchain-app-types');
  });
});
